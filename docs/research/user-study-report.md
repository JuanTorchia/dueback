# User Study Report

Status: **not yet run**. Denominator: 0 participants. No SC-001/SC-002 outcome is claimed.

The protocol and empty result schema are committed before recruitment to prevent changing measures
after observing results. When eight consented adult sessions are complete, this report must include
all P01–P08 rows, failures, denominators, timing distribution, near-verbatim comprehension answers,
problem-interview findings separately from usability outcomes, and comparison to every predeclared
threshold in [`user-study-protocol.md`](./user-study-protocol.md).

Do not mark T075 or T076 complete while this status remains `not yet run`.
